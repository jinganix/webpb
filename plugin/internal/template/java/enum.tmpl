// Code generated by Webpb compiler, do not edit.
// https://github.com/jinganix/webpb
// {{.filename}}

{{if hasContent .package}}package {{.package}};

{{end}}{{if .imports}}{{range .imports}}import {{.}};
{{end}}{{end}}{{if .msgAnnos}}{{range $i, $a := .msgAnnos}}{{if $i}}
{{else}}
{{end}}{{$a}}{{end}}{{else}}
{{end}}
public enum {{.className}}{{if hasContent .implements}} implements{{range $i, $impl := .implements}}{{if $i}}, {{else}} {{end}}{{$impl}}{{end}}{{end}} {

{{range $i, $enum := .enums}}  {{$enum.name}}({{$enum.value}}){{if not (last $i $.enums)}},
{{else}};{{end}}{{end}}

  private {{.valueType}} value;

  {{.className}}({{.valueType}} value) {
    this.value = value;
  }

  public static {{.className}} fromValue({{.valueType}} value) {
    switch (value) {
{{range .enums}}    case {{.value}}:
      return {{.name}};
{{end}}    default:
      return null;
    }
  }

  @Override
  public {{.valueType}} getValue() {
    return this.value;
  }
}
